---
# vim: foldmarker=[[[,]]]:foldmethod=marker

apt_install__group_packages:
  - 'borgbackup'

authorized_keys__force_options:
  - 'command="cd $HOME && borg serve --restrict-to-path $HOME"'
  - 'restrict'
