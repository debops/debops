---

# Allow use of the `dig +trace` command on the Unbound host

unbound__server:

  - name: 'access-control'
    value:

      - name: '127.0.0.0/8'
        args: 'allow_snoop'

      - name: '::1/128'
        args: 'allow_snoop'
