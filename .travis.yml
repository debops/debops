---

sudo: 'required'
language: 'python'
python: '2.7'

virtualenv:
  system_site_packages: true

before_install:
  - 'sudo apt-get -qq update'
  - 'sudo apt-get -yq install pycodestyle python-unittest2 python-nose2 python-nose2-cov'

install: True

script:
  - 'make tests'
