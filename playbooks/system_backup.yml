---

- hosts: all
  sudo: yes
  tags: system_backup

  tasks:
    - include: roles/sshd/tasks/backup.yml
    - include: roles/monkeysphere/tasks/backup.yml
    - include: roles/sslcerts/tasks/backup.yml

