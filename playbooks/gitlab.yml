---

- name: GitLab support
  hosts: ginas_gitlab
  sudo: yes

  roles:
    - { role: ginas.gitlab, tags: gitlab }

- name: GitLab CI support
  hosts: ginas_gitlab_ci
  sudo: yes

  roles:
    - { role: gitlab_ci, tags: gitlab_ci }

- name: GitLab CI Runner support
  hosts: ginas_gitlab_ci_runner
  sudo: yes

  roles:
    - { role: gitlab_ci_runner, tags: gitlab_ci_runner }

