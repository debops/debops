---

- name: RSYSLOG | Send cron log to separate file
  template: src=etc/rsyslog.d/cron.conf.j2 dest=/etc/rsyslog.d/cron.conf owner=root group=root backup=no
  notify: Restart rsyslogd
  tags:
    - rsyslog

