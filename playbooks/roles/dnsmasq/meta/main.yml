---

dependencies:

  - role: ferm
    when: dnsmasq_netboot is defined and dnsmasq_netboot
    tags: dnsmasq

  - role: tcpwrappers
    when: dnsmasq_netboot is defined and dnsmasq_netboot
    tags: dnsmasq

