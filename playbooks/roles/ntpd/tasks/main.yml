---

- name: Make sure openntpd is installed
  apt: pkg=openntpd state=latest install_recommends=no
  when: ansible_virtualization_type is defined and ansible_virtualization_type != 'lxc' or ntpd_enable is defined and ntpd_enable == True
  tags:
    - ntpd
    - packages

