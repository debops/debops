---

sks_recon_name: 'sks'
sks_recon_port: 11370

sks_server_services:
  - name: '{{ sks_recon_name }}'
    port: '{{ sks_recon_port }}'
    proto: 'tcp'
    comment: 'SKS Keyserver Reconcilliation Service'

