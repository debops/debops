---

dependencies:

  - role: debops.ferm
    when: dnsmasq_netboot is defined and dnsmasq_netboot
    tags: dnsmasq

  - role: debops.tcpwrappers
    when: dnsmasq_netboot is defined and dnsmasq_netboot
    tags: dnsmasq

