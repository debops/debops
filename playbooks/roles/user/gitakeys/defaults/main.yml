---

# Main site of the git repositories
user_gitakeys_github: 'https://github.com/'

# Repository group (usually username}
owner: $ENV(USER)

# Repository name
repository: 'pubkeys.git'

# File with SSH public keys
keyfile: 'ssh.pub'

# Branch to check out
branch: 'master'

