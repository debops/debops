---

# Default username
owner: 'root'

# Default dotfile hash to use
dotfile: 'drybjed'

# List of dotfile hashes
user_dotfiles:
  drybjed:
    git_repo: 'https://github.com/drybjed/dotfiles.git'
    command: 'make all dark'
    creates: '~/.zshenv.local'
    shell: '/bin/zsh'

