---
# role: secret (sensitive data management and storage)


# Absolute path to a directory with secret storage. Set it in
# 'inventory/group_vars/all.yml' to have the same base path in entire playbook
secret: False

# More configuration options can be found in the 'encfs' role. To have them
# only affect localhost, make sure to put 'encfs_*' variables in
# 'inventory/host_vars/localhost.yml'


