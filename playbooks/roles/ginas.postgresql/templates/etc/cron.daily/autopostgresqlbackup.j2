#!/bin/sh

if [ -x /usr/sbin/autopostgresqlbackup-{{ postgresql_version }}-{{ item.name | default('default') }} ]; then
        /usr/sbin/autopostgresqlbackup-{{ postgresql_version }}-{{ item.name | default('default') }}
fi
