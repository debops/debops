---

- name: Apply kernel shared memory configuration
  sysctl:
    sysctl_file: '{{ postgresql_server_sysctl_file }}'
    name: '{{ item.key }}'
    value: '{{ item.value }}'
    state: 'present'
    reload: True
  with_dict: postgresql_server_sysctl_values

