{% set sensu_server__tpl_admin_accounts = [] %}
{% if ansible_local|d() and ansible_local.core|d() and ansible_local.core.admin_users|d() %}
{%   for admin in ansible_local.core.admin_users %}
{%     set _ = sensu_server__tpl_admin_accounts.append({
  'name': admin,
  'groups': ['cluster-admins']
}) %}
{%   endfor %}
{% endif %}
{{ sensu_server__tpl_admin_accounts }}
