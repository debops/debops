---
# Copyright (C) 2026 Patryk Ściborek <patryk@sciborek.com>
# Copyright (C) 2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

# Custom tasks executed before the main docker_compose_service role tasks,
# after the 'secret' role is imported but before the combined service list
# is evaluated. Override at the project level by providing a file with the
# same relative path in the directory configured by 'override_paths.tasks_path'
# (by default 'ansible/overrides/tasks/docker_compose_service/pre_main.yml';
# resolved via the 'debops.debops.task_src' lookup plugin).
