---
# Copyright (C) 2015      Reto Gantenbein <reto.gantenbein@linuxmonk.ch>
# Copyright (C) 2017-2020 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2021-2026 David Härdeman <david@hardeman.nu>
# Copyright (C) 2015-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Import custom Ansible plugins
  ansible.builtin.import_role:
    name: 'ansible_plugins'

- name: Import DebOps global handlers
  ansible.builtin.import_role:
    name: 'global_handlers'

- name: Import DebOps secret role
  ansible.builtin.import_role:
    name: 'secret'

- name: Pre hooks
  ansible.builtin.include_tasks: "{{ lookup('debops.debops.task_src', 'dovecot/pre_main.yml') }}"

- name: Install Dovecot packages
  ansible.builtin.package:
    name: '{{ q("flattened", (["dovecot-core"]
                              + (["dovecot-imapd"]
                                 if ("imap" in dovecot__features or
                                     "imaps" in dovecot__features)
                                 else [])
                              + (["dovecot-pop3d"]
                                 if ("pop3" in dovecot__features or
                                     "pop3s" in dovecot__features)
                                 else [])
                              + (["dovecot-lmtpd"]
                                 if "lmtp" in dovecot__features
                                 else [])
                              + (["dovecot-mysql"]
                                 if "mysql" in dovecot__user_accounts
                                 else [])
                              + (["dovecot-pgsql"]
                                 if "pgsql" in dovecot__user_accounts
                                 else [])
                              + (["dovecot-sqlite"]
                                 if "sqlite" in dovecot__user_accounts
                                 else [])
                              + (["dovecot-ldap"]
                                 if "ldap" in dovecot__user_accounts
                                 else [])
                              + (["dovecot-gssapi"]
                                 if "gssapi" in dovecot__auth_mechanisms
                                 else [])
                              + (["dovecot-managesieved"]
                                 if "sieve" in dovecot__features
                                 else [])
                              + (["zstd"]
                                 if dovecot__snapshot_enabled
                                 else [])
                             )) }}'
    state: 'present'
  register: dovecot__register_packages
  until: dovecot__register_packages is succeeded

- name: Make sure that Ansible local facts directory exists
  ansible.builtin.file:
    path: '/etc/ansible/facts.d'
    state: 'directory'
    mode: '0755'

- name: Save Dovecot local facts
  ansible.builtin.template:
    src: 'etc/ansible/facts.d/dovecot.fact.j2'
    dest: '/etc/ansible/facts.d/dovecot.fact'
    mode: '0755'
  notify: [ 'Refresh host facts' ]
  tags: [ 'meta::facts' ]

- name: Uninstall disabled Dovecot protocols
  ansible.builtin.package:
    pkg: '{{ q("flattened", ((["dovecot-imapd"]
                              if ("imap" not in dovecot__features and
                                  "imaps" not in dovecot__features)
                              else [])
                             + (["dovecot-pop3d"]
                                if ("pop3" not in dovecot__features and
                                    "pop3s" not in dovecot__features)
                                else [])
                             + (["dovecot-lmtpd"]
                                if "lmtp" not in dovecot__features
                                else [])
                             + (["dovecot-mysql"]
                                 if "mysql" not in dovecot__user_accounts
                                 else [])
                             + (["dovecot-pgsql"]
                                if "pgsql" not in dovecot__user_accounts
                                else [])
                             + (["dovecot-sqlite"]
                                if "sqlite" not in dovecot__user_accounts
                                else [])
                             + (["dovecot-ldap"]
                                if ("ldap" not in dovecot__user_accounts)
                                else [])
                             + (["dovecot-managesieved"]
                                if "sieve" not in dovecot__features
                                else []))) }}'
    state: 'absent'
  notify: [ 'Restart dovecot' ]

- name: Remove old configuration files
  ansible.builtin.file:
    path: '/etc/dovecot/{{ item }}'
    state: 'absent'
  loop:
    - 'local.conf'
    - 'dovecot-ldap-passdb.conf'
    - 'dovecot-ldap-userdb.conf'
  notify: [ 'Restart dovecot' ]
  tags: [ "role::dovecot:conf" ]

- name: Generate separate Dovecot SQL configuration
  ansible.builtin.template:
    src: "{{ lookup('debops.debops.template_src', 'etc/dovecot/dovecot-sql.conf.ext.j2') }}"
    dest: '/etc/dovecot/dovecot-sql.conf.ext'
    owner: 'root'
    group: 'root'
    mode: '0600'
  notify: [ 'Restart dovecot' ]
  when: "dovecot__version is version('2.4.0', '<') and dovecot__user_accounts | d([]) | intersect(['mysql', 'pgsql', 'sqlite']) | length > 0"
  tags: [ "role::dovecot:conf", "role::dovecot:conf:sql" ]

- name: Remove separate Dovecot SQL configuration
  ansible.builtin.file:
    path: '/etc/dovecot/dovecot-sql.conf.ext'
    state: 'absent'
  notify: [ 'Restart dovecot' ]
  when: "dovecot__version is version('2.4.0', '>=') or dovecot__user_accounts | d([]) | intersect(['mysql', 'pgsql', 'sqlite']) | length == 0"
  tags: [ "role::dovecot:conf", "role::dovecot:conf:ldap" ]

- name: Generate separate Dovecot LDAP configuration
  ansible.builtin.template:
    src: "{{ lookup('debops.debops.template_src', 'etc/dovecot/dovecot-ldap-combined.conf.j2') }}"
    dest: '/etc/dovecot/dovecot-ldap-combined.conf'
    owner: 'root'
    group: 'root'
    mode: '0600'
  notify: [ 'Restart dovecot' ]
  when: "dovecot__version is version('2.4.0', '<') and 'ldap' in dovecot__user_accounts"
  tags: [ "role::dovecot:conf", "role::dovecot:conf:ldap" ]

- name: Remove separate Dovecot LDAP configuration
  ansible.builtin.file:
    path: '/etc/dovecot/dovecot-ldap-combined.conf'
    state: 'absent'
  notify: [ 'Restart dovecot' ]
  when: "dovecot__version is version('2.4.0', '>=') or 'ldap' not in dovecot__user_accounts"
  tags: [ "role::dovecot:conf", "role::dovecot:conf:ldap" ]

- name: Generate Dovecot deny user list
  ansible.builtin.template:
    src: "{{ lookup('debops.debops.template_src', 'etc/dovecot/dovecot.deny.j2') }}"
    dest: '/etc/dovecot/dovecot.deny'
    owner: 'root'
    group: 'root'
    mode: '0644'

- name: Create private directory to store passwdfile
  ansible.builtin.file:
    path: "{{ dovecot__passwdfile_path }}"
    state: directory
    owner: 'root'
    group: 'dovecot'
    mode: '0650'
  when: "'passwdfile' in dovecot__user_accounts"

- name: Make sure virtual mail user POSIX group exists
  ansible.builtin.group:
    name: '{{ dovecot__vmail_posix_group |
              d(dovecot__vmail_posix_user) }}'
    state: 'present'
    system: True
  when: 'dovecot__vmail_enabled | d(False)'
  tags: [ 'role::dovecot:user', 'role::dovecot:group' ]

- name: Make sure virtual mail user POSIX system account exists
  ansible.builtin.user:
    name: '{{ dovecot__vmail_posix_user }}'
    state: 'present'
    comment: 'Postfix Virtual Mail user'
    group: '{{ dovecot__vmail_posix_group |
               d(dovecot__vmail_posix_user) }}'
    home: '{{ dovecot__vmail_base }}'
    create_home: True
    system: True
    shell: '/usr/sbin/nologin'
    skeleton: null
  when: 'dovecot__vmail_enabled | d(False)'
  tags: [ 'role::dovecot:user' ]

- name: Make sure that the virtual mail user home directory exists - {{ dovecot__vmail_base }}
  ansible.builtin.file:
    dest: '{{ dovecot__vmail_base }}'
    state: 'directory'
    owner: '{{ dovecot__vmail_posix_user }}'
    group: '{{ dovecot__vmail_posix_group }}'
    mode: '0750'
  when: 'dovecot__vmail_enabled | d(False)'
  tags: [ 'role::dovecot:user' ]

- name: Make sure that PKI hook directory exists
  ansible.builtin.file:
    path: '{{ dovecot__pki_hook_path }}'
    state: 'directory'
    owner: 'root'
    group: 'root'
    mode: '0755'
  when: dovecot__pki | bool

- name: Manage PKI dovecot hook
  ansible.builtin.template:
    src: 'etc/pki/hooks/dovecot.j2'
    dest: '{{ dovecot__pki_hook_path + "/" + dovecot__pki_hook_name }}'
    owner: 'root'
    group: 'root'
    mode: '0755'
  when: dovecot__pki | bool

- name: Ensure the PKI dovecot hook is absent
  ansible.builtin.file:
    path: '{{ dovecot__pki_hook_path + "/" + dovecot__pki_hook_name }}'
    state: 'absent'
  when: not (dovecot__pki | bool)

- name: Divert original Dovecot main configuration file
  debops.debops.dpkg_divert:
    path: '/etc/dovecot/dovecot.conf'
  notify: [ 'Restart dovecot' ]
  tags: [ "role::dovecot:conf" ]

- name: Generate Dovecot main configuration file
  ansible.builtin.template:
    src: "{{ lookup('debops.debops.template_src', 'etc/dovecot/dovecot.conf.j2') }}"
    dest: '/etc/dovecot/dovecot.conf'
    owner: 'root'
    group: 'dovecot'
    mode: '0640'
  notify: [ 'Restart dovecot' ]
  tags: [ "role::dovecot:conf" ]

- name: Install helper scripts
  ansible.builtin.copy:
    src: 'usr/local/sbin/'
    dest: '/usr/local/sbin/'
    mode: '0755'
  tags: [ 'role::dovecot:backup' ]

- name: Generate backup configuration file
  ansible.builtin.template:
    src: 'etc/default/debops-dovecot-snapshot.j2'
    dest: '/etc/default/debops-dovecot-snapshot'
    mode: '0644'
  tags: [ 'role::dovecot:backup' ]

- name: Configure backup cron jobs
  ansible.builtin.cron:
    name: 'Create {{ item }} backup snapshots of Dovecot mailboxes'
    special_time: '{{ item }}'
    cron_file: 'debops-dovecot-snapshot'
    user: 'root'
    state: '{{ "present" if (dovecot__snapshot_enabled and
                             item in dovecot__snapshot_cron_jobs)
               else "absent" }}'
    job: '/usr/local/sbin/debops-dovecot-snapshot {{ item }}'
  loop: [ 'daily', 'weekly', 'monthly' ]
  loop_control:
    label: '{{ {"state": ("present" if (dovecot__snapshot_enabled and
                                        item in dovecot__snapshot_cron_jobs)
                          else "absent"),
                "cron_job": item} }}'
  tags: [ 'role::dovecot:backup' ]

- name: Update Ansible facts and restart dovecot, if necessary
  ansible.builtin.meta: 'flush_handlers'

- name: Generate sync script configuration file
  ansible.builtin.template:
    src: 'etc/default/debops-dovecot-sync.j2'
    dest: '/etc/default/debops-dovecot-sync'
    mode: '0644'
  when: ("dsync" in dovecot__features and dovecot__version is version("2.4.0", ">="))
  tags: [ 'role::dovecot:sync' ]

- name: Remove sync script configuration file
  ansible.builtin.file:
    dest: '/etc/default/debops-dovecot-sync'
    state: 'absent'
  when: ("dsync" not in dovecot__features or dovecot__version is version("2.4.0", "<"))
  tags: [ 'role::dovecot:sync' ]

- name: Generate sync script systemd unit file
  ansible.builtin.template:
    src: 'etc/systemd/system/debops-dovecot-sync.service.j2'
    dest: '/etc/systemd/system/debops-dovecot-sync.service'
    mode: '0644'
  when: (dovecot__version is version("2.4.0", ">="))
  register: dovecot__register_systemd_unit_file
  tags: [ 'role::dovecot:sync' ]

- name: Ensure that the sync script systemd service has the right state
  ansible.builtin.systemd:
    name: 'debops-dovecot-sync'
    state: '{{ "started" if ("dsync" in dovecot__features and
                             dovecot__version is version("2.4.0", ">=") and
                             dovecot__dsync_script_enabled)
               else "stopped" }}'
    enabled: '{{ True if ("dsync" in dovecot__features and
                          dovecot__version is version("2.4.0", ">=") and
                          dovecot__dsync_script_enabled)
                 else False }}'
    masked: False
    daemon_reload: '{{ dovecot__register_systemd_unit_file is changed }}'
  when: (dovecot__version is version("2.4.0", ">="))
  tags: [ 'role::dovecot:sync' ]

- name: Post hooks
  ansible.builtin.include_tasks: "{{ lookup('debops.debops.task_src', 'dovecot/post_main.yml') }}"
