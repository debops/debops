---

dependencies:

  - role: debops.ansible_plugins

  - role: debops.secret

galaxy_info:

  role_name: 'borgbackup'
  author: 'Robin Schneider'
  description: 'Setup backup using borgbackup and borgmatic'
  company: 'DebOps'
  license: 'GPL-3.0-only'
  min_ansible_version: '2.3.0'
  platforms:
    - name: Ubuntu
      versions:
        - xenial
    - name: Debian
      versions:
        - stretch
        - buster
  galaxy_tags:
    - system
    - backup
    - borg
    - borgbackup
