---

dependencies:

  - role: debops.ansible_plugins

galaxy_info:

  role_name: 'dhcp_probe'
  author: 'Maciej Delmanowski'
  description: 'Configure DHCP Probe service'
  company: 'DebOps'
  license: 'GPL-3.0-only'
  min_ansible_version: '2.6.0'

  platforms:

    - name: 'Ubuntu'
      versions:
        - bionic

    - name: 'Debian'
      versions:
        - stretch
        - buster

  galaxy_tags:
    - dhcp
    - networking
