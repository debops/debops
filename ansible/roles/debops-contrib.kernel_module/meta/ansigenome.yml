---

ansigenome_info:
  galaxy_url: 'https://galaxy.ansible.com/debops-contrib/kernel_module'
  github_url: 'https://github.com/debops-contrib/ansible-kernel_module'
  git_branch: 'master'

  travis: True
  ## Only testing the blacklisting feature because `modprobe` might not work
  ## depending on the virtualization/container Plattform involved.

  license_url: 'https://tldrlegal.com/license/gnu-general-public-license-v3-(gpl-3)'

  authors:

    - name:  'Robin Schneider'
      email: 'ypid@riseup.net'
      github: 'ypid'

  synopsis: |
    ### Features

    * Module blacklisting
    * Module loading (with optional parameters)
    * Optionally forces that a module is loaded with certain parameters by unloading it first.
    * Either make changes permanent or only to the running system. Default is permanent.
