---

dependencies:

  - role: debops.secret
    tags: [ 'role::secret' ]

  - role: debops.ansible_plugins

galaxy_info:

  role_name: 'discourse'
  author: 'Hartmut Goebel'
  description: 'Install, upgrade and manage Discourse forum instance'
  company: 'DebOps'
  license: 'GPL-3.0-only'
  min_ansible_version: '2.1.0'
  platforms:
    - name: Debian
      versions:
        - buster
  galaxy_tags:
    - discourse
