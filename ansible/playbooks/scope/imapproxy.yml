---
# Copyright (C) 2021 David Härdeman <david@hardeman.nu>
# Copyright (C) 2021-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Install and manage imapproxy
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_imapproxy' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import imapproxy role
      ansible.builtin.import_role:
        name: 'debops.debops.imapproxy'
      tags: [ 'role::imapproxy', 'skip::imapproxy' ]
