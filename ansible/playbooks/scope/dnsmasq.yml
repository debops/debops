---
# Copyright (C) 2014-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2015-2017 Robin Schneider <ypid@riseup.net>
# Copyright (C) 2014-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Configure dnsmasq
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_dnsmasq' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  pre_tasks:

    - name: Prepare dnsmasq environment
      ansible.builtin.import_role:
        name: 'debops.debops.dnsmasq'
        tasks_from: 'main_env'
      tags: [ 'role::dnsmasq', 'role::dnsmasq:env' ]

  tasks:

    - name: Import dnsmasq role
      ansible.builtin.import_role:
        name: 'debops.debops.dnsmasq'
      tags: [ 'role::dnsmasq', 'skip::dnsmasq' ]
