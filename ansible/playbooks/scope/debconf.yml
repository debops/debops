---
# Copyright (C) 2024-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2024-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-or-later

- name: Manage debconf-based services
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_debconf' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import debconf role
      ansible.builtin.import_role:
        name: 'debops.debops.debconf'
      tags: [ 'role::debconf', 'skip::debconf' ]
