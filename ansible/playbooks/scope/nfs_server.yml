---
# Copyright (C) 2017-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2017-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Configure NFS Server
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_nfs_server' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import nfs_server role
      ansible.builtin.import_role:
        name: 'debops.debops.nfs_server'
      tags: [ 'role::nfs_server', 'skip::nfs_server' ]
