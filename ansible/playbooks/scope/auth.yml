---
# Copyright (C) 2013-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2014-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Manage authentication and authorization
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_hosts', 'debops_all_services', 'debops_service_auth' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import auth role
      ansible.builtin.import_role:
        name: 'debops.debops.auth'
      tags: [ 'role::auth', 'skip::auth' ]
