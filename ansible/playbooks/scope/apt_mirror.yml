---
# Copyright (C) 2023-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2023-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Configure APT mirroring service
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_apt_mirror' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import apt_mirror role
      ansible.builtin.import_role:
        name: 'debops.debops.apt_mirror'
      tags: [ 'role::apt_mirror', 'skip::apt_mirror' ]
