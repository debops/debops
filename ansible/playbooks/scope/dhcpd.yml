---
# Copyright (C) 2014-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2020 CipherMail B.V. <https://www.ciphermail.com/>
# Copyright (C) 2014-2026, 2020 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Manage ISC DHCP server
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_dhcpd' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import dhcpd role
      ansible.builtin.import_role:
        name: 'debops.debops.dhcpd'
      tags: [ 'role::dhcpd', 'skip::dhcpd' ]
