---
# Copyright (C) 2016-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2016-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Manage GitLab Runner service
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_gitlab_runner' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import gitlab_runner role
      ansible.builtin.import_role:
        name: 'debops.debops.gitlab_runner'
      tags: [ 'role::gitlab_runner', 'skip::gitlab_runner' ]
