---
# Copyright (C) 2019 Rainer 'rei' Schuth <devel@reixd.net>
# Copyright (C) 2019-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Manage Postfix service with Virtual Mail LDAP backend
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_postldap' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import postldap role
      ansible.builtin.import_role:
        name: 'debops.debops.postldap'
      tags: [ 'role::postldap', 'skip::postldap' ]
