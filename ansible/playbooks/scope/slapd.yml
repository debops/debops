---
# Copyright (C) 2016-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2016-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Manage OpenLDAP service
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_slapd' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import slapd role
      ansible.builtin.import_role:
        name: 'debops.debops.slapd'
      tags: [ 'role::slapd', 'skip::slapd' ]
