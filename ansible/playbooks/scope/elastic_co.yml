---
# Copyright (C) 2017-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2017-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Manage Elastic APT repositories
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_elastic_co' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import elastic_co role
      ansible.builtin.import_role:
        name: 'debops.debops.elastic_co'
      tags: [ 'role::elastic_co', 'skip::elastic_co' ]
