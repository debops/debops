---
# Copyright (C) 2018-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2018-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Configure Icinga Web service
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_icinga_web' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import icinga_web role
      ansible.builtin.import_role:
        name: 'debops.debops.icinga_web'
      tags: [ 'role::icinga_web', 'skip::icinga_web' ]
