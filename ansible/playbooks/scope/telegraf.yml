---
# Copyright (C) 2021 Dr. Serge Victor <https://dr.sergevictor.eu/>
# Copyright (C) 2021-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-or-later

- name: Manage Telegraf instance
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_telegraf' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import telegraf role
      ansible.builtin.import_role:
        name: 'debops.debops.telegraf'
      tags: [ 'role::telegraf', 'skip::telegraf' ]
