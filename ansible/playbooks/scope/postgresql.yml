---
# Copyright (C) 2015-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2015-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Manage PostgreSQL client
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_postgresql' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import postgresql role
      ansible.builtin.import_role:
        name: 'debops.debops.postgresql'
      tags: [ 'role::postgresql', 'skip::postgresql' ]
