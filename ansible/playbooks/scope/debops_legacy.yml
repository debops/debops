---
# Copyright (C) 2018-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2018-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Clean up legacy configuration
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_hosts', 'debops_all_services' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import debops_legacy role
      ansible.builtin.import_role:
        name: 'debops.debops.debops_legacy'
      tags: [ 'role::debops_legacy', 'skip::debops_legacy' ]
