---
# Copyright (C) 2015-2020 Robin Schneider <ypid@riseup.net>
# Copyright (C) 2015-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Setup and manage encrypted filesystems
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_cryptsetup' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import cryptsetup role
      ansible.builtin.import_role:
        name: 'debops.debops.cryptsetup'
      tags: [ 'role::cryptsetup', 'skip::cryptsetup' ]
