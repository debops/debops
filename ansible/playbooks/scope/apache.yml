---
# Copyright (C) 2016-2017 Robin Schneider <ypid@riseup.net>
# Copyright (C) 2016-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-only

- name: Manage and configure the Apache HTTP Server
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_apache' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  pre_tasks:

    - name: Prepare apache environment
      ansible.builtin.import_role:
        name: 'debops.debops.apache'
        tasks_from: 'main_env'
      tags: [ 'role::apache', 'role::apache:env' ]

  tasks:

    - name: Import apache role
      ansible.builtin.import_role:
        name: 'debops.debops.apache'
      tags: [ 'role::apache', 'skip::apache' ]
