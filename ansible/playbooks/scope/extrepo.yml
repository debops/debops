---
# Copyright (C) 2021-2026 Maciej Delmanowski <drybjed@gmail.com>
# Copyright (C) 2021-2026 DebOps <https://debops.org/>
# SPDX-License-Identifier: GPL-3.0-or-later

- name: Manage external APT sources
  collections: [ 'debops.debops' ]
  hosts: [ 'debops_all_services', 'debops_service_extrepo' ]
  become: True

  environment: '{{ inventory__environment | d({})
                   | combine(inventory__group_environment | d({}))
                   | combine(inventory__host_environment  | d({})) }}'

  tasks:

    - name: Import extrepo role
      ansible.builtin.import_role:
        name: 'debops.debops.extrepo'
      tags: [ 'role::extrepo', 'skip::extrepo' ]
