---

- name: Configure encrypted filesystems using cryptsetup
  hosts: 'debops_cryptsetup'
  become: True

  roles:

    - role: debops.contrib-cryptsetup
      tags: [ 'role::cryptsetup', 'cryptsetup' ]
